<%= form_for(@song_request) do |f| %>
  <% if @song_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_request.errors.count, "error") %> prohibited this song_request from being saved:</h2>

      <ul>
      <% @song_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table width="100%">
  <tr>
    <td>
      <div class="field">
        <%= f.label :dedicated_to %><br>
        <%= f.text_field :dedicated_to %>
      </div>
    </td>
    <td>
      <div class="field">
        <%= f.label :song_url %><br>
        <%= f.text_field :song_url %>
      </div>
    </td>
  </tr>
  <tr>
    <td>
      <div class="field">
        <%= f.label :message %><br>
        <%= f.text_area :message %>
      </div>
    </td>
    <td>
      <div class="field">
        <%= f.label :requestor %><br>
        <%= f.text_field :requestor %>
      </div>
    </td>
  </tr>
</table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>